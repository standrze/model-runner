diff --git a/mlx/c/stream.cpp b/mlx/c/stream.cpp
--- a/mlx/c/stream.cpp
+++ b/mlx/c/stream.cpp
@@ -80,6 +80,15 @@ extern "C" int mlx_synchronize(mlx_stream stream) {
   }
   return 0;
 }
+extern "C" int mlx_clear_streams(void) {
+  try {
+    mlx::core::clear_streams();
+  } catch (std::exception& e) {
+    mlx_error(e.what());
+    return 1;
+  }
+  return 0;
+}
 extern "C" int mlx_get_default_stream(mlx_stream* stream, mlx_device dev) {
   try {
     mlx_stream_set_(*stream, mlx::core::default_stream(mlx_device_get_(dev)));
diff --git a/mlx/c/stream.h b/mlx/c/stream.h
--- a/mlx/c/stream.h
+++ b/mlx/c/stream.h
@@ -62,6 +62,10 @@ int mlx_stream_get_index(int* index, mlx_stream stream);
  */
 int mlx_synchronize(mlx_stream stream);
 /**
+ * Synchronize and release stream command encoders owned by this thread.
+ */
+int mlx_clear_streams(void);
+/**
  * Returns the default stream on the given device.
  */
 int mlx_get_default_stream(mlx_stream* stream, mlx_device dev);
